$("#company_<%= @company.id %>").html("<%= escape_javascript(render :partial => 'item', :object => @company) %>");
$("#companies .company:last .item").addClass("last-item");
replaceFonts("h1, h6, .item h2, h2.cv-section-name");
bindRemoteLinks("#form-dialog", "#companies #company_<%= @company.id %> .remote-link");
