$("#form-dialog").dialog("open");
$(".ui-dialog-content").html("<%= escape_javascript(render :partial => 'form', :locals => {:person => @person, :company => @company, :command => controller.action_name.capitalize}) %>");
focusOnFirstFormTextField("#form-dialog");
bindFormButtons("#form-dialog");
