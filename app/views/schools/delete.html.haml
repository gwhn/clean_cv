.cv-section.last-cv-section.fixed
  %h2.cv-section-name Delete school
  .cv-section-items
    - semantic_form_for [@person, @school], :html => { :method => 'delete' } do |f|
      %p Are you sure you want to delete #{@school.name}?
      - f.buttons do
        = f.commit_button 'Yes'

    - if permitted_to? :read, @person
      = link_to 'Back', @person
