$("#schools .school:last .item").removeClass("last-item");
$("<%= escape_javascript(render :partial => 'item', :object => @school) %>").appendTo("#schools");
$("#schools .school:last .item").addClass("last-item");
replaceFonts("h1, h6, .item h2, h2.cv-section-name");
bindRemoteLinks("#form-dialog", "#schools #school_<%= @school.id %> .remote-link");
