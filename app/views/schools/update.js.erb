<%- school_id = "#school_#{@school.id}" %>
$("<%= school_id %>").replaceWith("<%= escape_javascript(render :partial => 'item', :object => @school) %>");
bindRemoteLinks("#form-dialog", ".remote-link");
$("<%= school_id %>").effect("highlight");
