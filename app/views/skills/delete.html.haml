.cv-section.last-cv-section.fixed
  %h2.cv-section-name Delete skill
  .cv-section-items
    - semantic_form_for [@person, @skill], :html => { :method => 'delete' } do |f|
      %p Are you sure you want to delete #{@skill.name}?
      - f.buttons do
        = f.commit_button 'Yes'
