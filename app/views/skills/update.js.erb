<%- skill_id = "#skill_#{@skill.id}" -%>
$("<%= skill_id %>").replaceWith("<%= escape_javascript(render :partial => 'item', :object => @skill) %>");
<%= dynamic_tooltip_javascript_for "#{skill_id} > .skill", 'top right' %>
bindRemoteLinks("#form-dialog", ".remote-link");
$(".move-actions").hide();
$("<%= skill_id %>").effect("highlight");
