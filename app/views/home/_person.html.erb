<div class="person">
  <a class="lightbox" rel="mypics" href="<%= h person.portrait_url %>" title="<%= h person.name %>">
    <img src="<%= h person.portrait_thumb_url %>" width="100" height="100" alt="<%= h person.name %>"/>
  </a>

  <h1><%= h person.name %></h1>

  <h2><%= h person.job_title %></h2>
</div>