.cv-section.last-cv-section.fixed
  %h2.cv-section-name Delete project
  .cv-section-items
    - semantic_form_for [@person, @company, @project], :html => { :method => 'delete' } do |f|
      %p Are you sure you want to delete #{@project.name}?
      - f.buttons do
        = f.commit_button 'Yes'
