$("#person_<%= @person.id %>").replaceWith("<%= escape_javascript(render :partial => 'item', :object => @person) %>");
initFancyBox("a.lightbox");
bindRemoteLinks("#form-dialog", "#cv #person_<%= @person.id %> .remote-link");

