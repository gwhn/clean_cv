$("#person_<%= @person.id %>").html("<%= escape_javascript(render :partial => 'item', :object => @person) %>");
replaceFonts("h1, h6, .item h2, h2.cv-section-name");
bindRemoteLinks("#form-dialog", "#cv #person_<%= @person.id %> .remote-link");

