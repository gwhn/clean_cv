%div{:id => ['person', item.id]}
  .cv-section.first-cv-section.fixed
    = render :partial => 'person', :object => item
    = render :partial => 'contact', :object => item

    - if current_user
      .tr-options
        - if !current_page?(item)
          = link_to 'View', item
          |
        = link_to 'Edit', edit_person_path(item), :class => 'remote-link', :title => 'Edit person'
        |
        = link_to 'Destroy', item, :confirm => 'Are you sure?', :method => :delete
    - elsif !current_page?(item)
      .tr-options
        = link_to 'View', item

  .cv-section.fixed
    %h2.cv-section-name Professional Profile
    .cv-section-items
      = render :partial => 'profile', :object => item

