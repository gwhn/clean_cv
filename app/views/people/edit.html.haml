%h1 Editing person
= render :partial => 'form', :locals => {:person => @person, :url => person_url(@person, :format => :js)}

- if permitted_to? :read, @person
  = link_to 'Show', @person
  = link_to 'Back', people_path
