.cv-section.fixed
  %h2.cv-section-name Resend unlock instructions
  .cv-section-items
    - semantic_form_for resource_name, resource, :url => unlock_path(resource_name) do |f|
      - f.inputs do
        = f.input :email

      - f.buttons do
        = f.commit_button 'Resend unlock instructions'

= render :partial => 'shared/options'
